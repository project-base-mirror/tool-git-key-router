# GitKeyRouter

GitKeyRouter is a Windows desktop utility for managing multiple GitHub SSH identities, OpenSSH host aliases, and Git `url.*.insteadOf` owner routing rules.

> This repository is under initial implementation. See `docs/architecture.md` for the design and safety boundaries.
